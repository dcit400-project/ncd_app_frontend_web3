[build]
  command = "npm run build"
  functions = "netlify/functions"
  publish = "." # not serving a frontend

[dev]
  port = 8888

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/server"
  status = 200
